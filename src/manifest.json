{
  "name": "SAML SSO Helper (Beta)",
  "description": "Helper extension for admins to configure SAML SSO for Chrome apps.",
  "version": "0.7",
  "manifest_version": 2,
  "storage": {
    "managed_schema": "schema.json"
  },
  "permissions": [
    "cookies",
    "storage",
    "https://*/*"
  ],
  "background": {
    "scripts": ["background.js"]
  }
}
